<h1>投稿一覧</h1>

<% @lists.each do |list| %>

  <p>タイトル</p>
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  <!--as:オプションを追加「"/lists/#{list.id}"」を「list_path(list.id)」に書き換え-->

<% end %>
